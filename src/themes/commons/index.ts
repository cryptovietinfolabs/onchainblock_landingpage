import { ComponentStyleConfig } from "@chakra-ui/react";

export const commonBadge: ComponentStyleConfig = {
  baseStyle: {
    textTransform: "uppercase",
    fontWeight: "semibold",
    letterSpacing: "0.02em",
    padding: "4px",
    borderRadius: "2px",
    fontSize: "22px",
  },
};
